<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MemoWithLogStamp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:textColor">@color/black</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="ActivityBackground" parent="TextAppearance.AppCompat">
        <item name="android:background">@color/background</item>
    </style>
    <style name="TextViewDefault" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="EditTextDefault" parent="TextAppearance.AppCompat">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.MemoWithLogStamp" parent="Base.Theme.MemoWithLogStamp" />
</resources>